{
  "name": "noro",
  "version": "0.3.1",
  "description": "share env vars with one command",
  "type": "module",
  "bin": {
    "noro": "dist/index.js"
  },
  "files": [
    "dist",
    "docs"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/crypto.ts --format esm --dts",
    "dev": "tsup src/index.ts src/crypto.ts --format esm --watch",
    "test": "bun test"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
